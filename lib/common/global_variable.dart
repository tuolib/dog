import 'package:socket_io_client/socket_io_client.dart';import '../index.dart';// 打印 调试var logger = Logger(  // printer: PrettyPrinter(  //     methodCount: 2, // number of method calls to be displayed  //     errorMethodCount: 8, // number of method calls if stacktrace is provided  //     lineLength: 120, // width of the output  //     colors: true, // Colorful log messages  //     printEmojis: true, // Print an emoji for each log message  //     printTime: false // Should each log print contain a timestamp  // ),  // filter: null,  // output: null,);SocketsIoNotify socketIoItem = new SocketsIoNotify();Socket socketInit;// 是否发送过 clientOpenGroupbool isSend = false;// 当进入聊天页面， 赋值Provider.of<ConversationListModel>(context)，// socketInit.on('serverSendGroupMessage' 内部就可以使用ConversationListModel里面的方法ConversationListModel socketProviderConversationListModel;ChatInfoModel chatInfoModelSocket;CallInfoModel callInfoSocket;var socketProviderConversationListModelGroupId;// 首页 / chats 列表页面ChatListModel socketProviderChatListModel;//正在打开的群 idvar socketProviderChatListModelGroupId;var currentPage;// 联系人页面ContactListModel socketProviderContactListModel;// 新建群详情页面AddPeopleModel socketProviderAddPeopleModel;GroupMemberModel socketGroupMemberModel;//从A群信息页面进入个人信息B页面，保存的A群信息var personFromGroupBefore;// 是否在连接中ConnectSocketModel socketProviderConnectModel;// 首页chats是否在更新中bool isUpdatingGroup = false;// 某个对话是否在更新中bool isUpdatingConversation = false;// 路由监听final RouteObserver<PageRoute> routeObserver = RouteObserver<PageRoute>();bool backingRoute = false;// ios file pathString iosFilePre;